const GOM = require('../../core/game-object-manager');
const CONFIG = require('../game-config');

class Menu {
    constructor () {
        this.setEvents();
    }

    setEvents () {

    }
}

module.exports = new Menu();
